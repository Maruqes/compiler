func test(ptr p, ptr p2, ptr a){
	printHex(p);
	printHex(p2);
	printHex(***a);
	return;
}

func main(){
	dq a = 0x0123456789abcdef;
	dq b = 0x0123456789abcdef;

	ptr d = &a;

	ptr e = &d;

	test(d, &b, &e);

	return;
}